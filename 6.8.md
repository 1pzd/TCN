# 6.8 TCN 模型缩小方向检查记录

## 用户问题

当前工程是一个 TCN 时序分类模型，但 clip 级和受试者级 AUC/ACC 都比较一般，大约 60%。本次检查目标是：从“减小模型”的角度，寻找可能的解决方法。

## 当前工程关键结论

### 1. 主模型明显过大

主模型位于：

- `src/model.py`
- `configs/config.yaml`

检查时的原始主要配置为：

```yaml
num_channels: [64, 96, 128, 192, 256, 320, 384, 448]
kernel_sizes: [5, 5, 5, 3, 3, 3, 3, 3]
dropout: 0.1
```

输入特征只有：

```text
gaze_x, gaze_y
```

也就是 2 个输入通道，但原始模型包含：

- 8 层 TCN
- 最大通道数 448
- 每层 2 个 Conv1d
- 每层 SEBlock
- 多层特征融合：layer 2 / 4 / 7
- GAP + GMP 拼接
- 大分类头：约 1664 → 832 → 2

估计参数量约：

```text
5.26M
```

其中：

```text
TCN 主干：约 3.87M
分类头：约 1.39M
```

对于当前数据规模，这个模型非常容易过拟合。

### 2. 数据规模较小

数据处理后大约为：

```text
48 个受试者
960 个 clip
每个受试者 20 个 clip
BJ: 27 subjects, 540 clips
ZJ: 21 subjects, 420 clips
```

3-fold CV 后，每折训练大约只有：

```text
约 32 个受试者
约 640 个 clip
```

用约 640 个 clip 训练 5.26M 参数模型，泛化能力很难稳定。当前 60% 左右的 AUC/ACC 更像是模型容量过大、正则不足、受试者级泛化不稳定，而不是模型表达能力不够。

### 3. TCN 感受野远超序列长度

当前：

```text
max_seq_len = 200
```

但原始 8 层 TCN 的完整感受野约为：

```text
1049 time steps
```

这已经是输入长度的 5 倍以上。后几层 dilation=64、128 的卷积很可能在学习 padding 或过度稀疏的上下文，对有效时序建模帮助有限，反而增加参数和过拟合风险。

### 4. 分类头过大

原始多层融合后：

```text
fusion dim = (128 + 256 + 448) * 2 = 1664
```

然后经过：

```text
LayerNorm(1664)
Linear(1664, 832)
Linear(832, 2)
```

原始单分类头约 1.39M 参数。对于二分类任务，这个分类头明显过大。

### 5. 训练协议和正则也偏弱

当前训练配置：

```yaml
dropout: 0.1
weight_decay: 1e-5
patience: 100
class_weight: null
```

问题：

- `dropout=0.1` 对当前大模型太弱。
- `weight_decay=1e-5` 正则强度偏低。
- `patience=100` 基本等于没有有效 early stopping。
- 训练中 test fold 被用于 scheduler、early stopping 和 best model selection，缺少独立 validation set。

## 建议的缩小方向

### 阶段 1：先只改配置，不改模型结构

这是本次实际执行的阶段。目标是在不改代码结构的情况下，先增强正则和早停：

```yaml
dropout: 0.3
weight_decay: 1e-3
patience: 15
```

本阶段不修改：

- `num_channels`
- `kernel_sizes`
- `src/model.py`
- `fusion_levels`
- SEBlock
- 分类头结构

### 阶段 2：缩小 TCN 主干

如果阶段 1 后指标仍不稳定，建议将模型缩到 5 层左右：

```yaml
num_channels: [32, 48, 64, 96, 128]
kernel_sizes: [5, 5, 3, 3, 3]
dropout: 0.3
```

同时需要把 `src/model.py` 中的 `fusion_levels` 从当前 `[2, 4, 7]` 改成适配 5 层模型的配置，例如：

```python
fusion_levels = [1, 3, 4]
```

### 阶段 3：简化分类头

当前分类头过大。后续可以考虑：

```text
final GAP → Linear → 2
```

或小 MLP：

```text
N → 128 → 2
```

### 阶段 4：去掉或弱化 SEBlock

SEBlock 在当前 2 输入特征、小样本数据下可能收益有限。后续可以尝试：

- 完全移除 SEBlock
- 或只在最后 1-2 层使用 SEBlock

### 阶段 5：增加输入标准化和独立验证集

数据管线中目前没有对 `gaze_x/gaze_y` 做 normalization。后续建议加入 per-clip 或 train-set 统计标准化。

同时建议将训练协议改为：

```text
train subjects -> train/validation
test subjects -> 只用于最终评估
```

避免用 test fold 进行 early stopping 和 best model selection。

## 已执行内容

### 阶段 1：已执行

阶段 1 只改训练正则配置，不改模型结构。修改内容为：

```yaml
model.dropout: 0.1 -> 0.3
training.weight_decay: 1.0e-5 -> 1.0e-3
training.patience: 100 -> 15
```

阶段 1 未修改模型结构、通道数、卷积核、SEBlock、多层融合或分类头。

### 阶段 2：已执行

阶段 2 已将主 TCN 从 8 层缩小为 5 层，并同步适配多层融合索引。修改内容为：

```yaml
model.num_channels: [64, 96, 128, 192, 256, 320, 384, 448] -> [32, 48, 64, 96, 128]
model.kernel_sizes: [5, 5, 5, 3, 3, 3, 3, 3] -> [5, 5, 3, 3, 3]
model.dropout: 0.3  # 保持阶段 1 设置
```

```python
self.fusion_levels: [2, 4, len(num_channels) - 1] -> [1, 3, 4]
```

阶段 2 后的融合维度为：

```text
fusion dim = (48 + 96 + 128) * 2 = 544
classifier: LayerNorm(544) -> Linear(544, 272) -> Linear(272, 2)
```

预计模型参数量将从约 5.26M 降到约 35 万级别。旧的 `output_model/*.pth` 检查点保存了旧架构参数，和新 5 层结构不兼容；需要重新运行 `python train.py` 生成新检查点。

### 阶段 3：6 层中间容量试验

阶段 2 的 5 层模型训练结果变差，说明 5 层配置可能压缩过强。当前先不回到原始 8 层大模型，而是改为 6 层中间容量试验：

```yaml
model.num_channels: [32, 48, 64, 96, 128] -> [32, 48, 64, 96, 128, 160]
model.kernel_sizes: [5, 5, 3, 3, 3] -> [5, 5, 3, 3, 3, 3]
model.dropout: 0.3  # 保持阶段 1 设置
```

```python
self.fusion_levels: [1, 3, 4] -> [1, 3, 5]
```

6 层试验后的融合维度为：

```text
fusion dim = (48 + 96 + 160) * 2 = 608
classifier: LayerNorm(608) -> Linear(608, 304) -> Linear(304, 2)
```

本配置预计参数量约 56 万级别，比 5 层模型更大，但仍远小于原始 8 层约 5.26M 参数模型。完成修改后需要重新运行 `python train.py`，不要直接复用旧的 `output_model/*.pth` 检查点。

## 一句话总结

当前 60% 左右的 clip 级和受试者级 AUC/ACC，更可能不是因为模型太小，而是因为模型对当前 48 个受试者、2 个输入特征的数据来说太大。建议先从强正则和早停开始，再逐步把模型压到 100K-300K 参数级别。
